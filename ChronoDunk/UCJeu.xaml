<UserControl x:Class="ChronoDunk.UCJeu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" Width="1280"
             Focusable="True"
             KeyDown="OnKeyDown" KeyUp="OnKeyUp"
             MouseDown="OnMouseDown" MouseUp="OnMouseUp" MouseMove="OnMouseMove">

    <UserControl.Resources>
        <SolidColorBrush x:Key="ColorPrimary" Color="#FFFF6F00"/>
        <SolidColorBrush x:Key="ColorAlert" Color="#FFFF5200"/>
        <SolidColorBrush x:Key="ColorTextYellow" Color="Yellow"/>
        <SolidColorBrush x:Key="ColorOverlay" Color="#CC000000"/>

        <FontFamily x:Key="FontIrish">./fonts/#Irish Grover</FontFamily>
        <FontFamily x:Key="FontConsolas">Consolas</FontFamily>

        <Style x:Key="StyleBtnPause" TargetType="Button">
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="White" BorderThickness="1" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.1" ScaleY="1.1"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Cursor" Value="Hand"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StyleBtnMenu" TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource FontIrish}"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource ColorPrimary}"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="Height" Value="55"/>
            <Setter Property="Width" Value="240"/>
            <Setter Property="Margin" Value="0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Background" Value="{StaticResource ColorAlert}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="Black">
        <Canvas x:Name="canvasJeu" Background="Transparent" MouseLeftButtonDown="GameCanvas_Click" RenderOptions.BitmapScalingMode="HighQuality">
            <Canvas.RenderTransform>
                <TranslateTransform x:Name="CanvasShake" X="0" Y="0"/>
            </Canvas.RenderTransform>

            <Image Source="/images/background.jpg" Stretch="Fill" Width="1280" Height="720" IsHitTestVisible="False"/>

            <StackPanel Canvas.Left="540" Canvas.Top="18" Width="200" HorizontalAlignment="Center">
                <Border Background="#E5000000" CornerRadius="5" Margin="0,0,0,25" HorizontalAlignment="Center" Padding="15,2">
                    <TextBlock x:Name="TimerMatch" Text="30" Foreground="#00FF00" FontSize="35" FontWeight="Bold" FontFamily="{StaticResource FontConsolas}"/>
                </Border>
                <Border Background="#E5000000" CornerRadius="5" HorizontalAlignment="Center" Padding="10,5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock x:Name="PlayerScoreText" Text="0" Foreground="{StaticResource ColorTextYellow}" FontSize="30" FontWeight="Bold" Margin="0,0,40,0"/>
                        <TextBlock Text="-" Foreground="White" FontSize="30" FontWeight="Bold"/>
                        <TextBlock x:Name="EnemyScoreText" Text="0" Foreground="Red" FontSize="30" FontWeight="Bold" Margin="40,0,0,0"/>
                    </StackPanel>
                </Border>
                <Grid Margin="0,2,0,0">
                    <TextBlock Text="TOI" Foreground="{StaticResource ColorTextYellow}" FontSize="10" HorizontalAlignment="Left" Margin="10,10,0,0" FontWeight="Bold"/>
                    <TextBlock Text="ADV" Foreground="Red" FontSize="10" HorizontalAlignment="Right" Margin="0,10,10,0" FontWeight="Bold"/>
                </Grid>
            </StackPanel>

            <ProgressBar x:Name="SuperBar" Width="200" Height="20" Canvas.Left="10" Canvas.Top="640" Background="Gray" Foreground="OrangeRed" Value="0" Maximum="100"/>
            <TextBlock Text="SUPER FEU" Canvas.Left="15" Canvas.Top="643" Foreground="White" FontWeight="Bold" FontSize="10" IsHitTestVisible="False"/>

            <Polyline x:Name="TrajectoireBalle" Stroke="Gray" StrokeThickness="5" StrokeDashArray="4 2" Opacity="0.8" IsHitTestVisible="False"/>

            <Rectangle x:Name="collisionPanierDroit" Fill="Red" Opacity="0" Width="34" Height="103" Canvas.Top="175" Canvas.Left="1075"/>
            <Rectangle x:Name="zoneScorePanierDroit" Fill="Lime" Opacity="0" Width="39" Height="10" Canvas.Top="273" Canvas.Left="1005"/>
            <Rectangle x:Name="collisionPanierGauche" Fill="Red" Opacity="0" Width="40" Height="100" Canvas.Top="178" Canvas.Left="183"/>
            <Rectangle x:Name="zoneScorePanierGauche" Fill="Lime" Opacity="0" Width="43" Height="6" Canvas.Top="275" Canvas.Left="238"/>

            <Image x:Name="Player" Source="/images/player.png" Height="300" Width="200" Canvas.Top="364" Canvas.Left="255" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <ScaleTransform ScaleX="1"/>
                </Image.RenderTransform>
            </Image>

            <Image x:Name="Enemy" Source="/images/player.png" Height="300" Width="200" Canvas.Top="364" Canvas.Left="835" IsHitTestVisible="False" Opacity="0.9" RenderTransformOrigin="0.5,0.5">
                <Image.Effect>
                    <DropShadowEffect Color="Red" BlurRadius="20" ShadowDepth="0"/>
                </Image.Effect>
                <Image.RenderTransform>
                    <ScaleTransform ScaleX="-1"/>
                </Image.RenderTransform>
            </Image>

            <Image x:Name="Balle" Source="/images/ballon.png" Height="80" Width="80" Canvas.Top="486" Canvas.Left="600" IsHitTestVisible="False" RenderTransformOrigin="0.5,0.5">
                <Image.RenderTransform>
                    <RotateTransform Angle="0"/>
                </Image.RenderTransform>
            </Image>

            <TextBlock x:Name="CountdownText" Text="" Width="1280" Canvas.Top="200" Canvas.Left="0" TextAlignment="Center" FontSize="120" FontWeight="Bold" Foreground="{StaticResource ColorTextYellow}" Visibility="Collapsed" IsHitTestVisible="False">
                <TextBlock.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="10"/>
                </TextBlock.Effect>
            </TextBlock>
        </Canvas>

        <Button x:Name="buttonPause" Click="PauseButton_Click" Style="{StaticResource StyleBtnPause}" Background="{StaticResource ColorPrimary}" Width="40" Height="40" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="26,27,0,0">
            <Image x:Name="imgPause" Source="/images/buttonPause.png" Stretch="Uniform"/>
        </Button>

        <Grid x:Name="GameOverScreen" Visibility="Collapsed" Background="{StaticResource ColorOverlay}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="FIN DU MATCH" Margin="0,0,0,10" Foreground="LightGray" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" FontFamily="{StaticResource FontIrish}"/>

                <TextBlock x:Name="ResultText" Text="VICTOIRE !" Margin="0,0,0,10" Foreground="Lime" FontSize="80" FontWeight="Bold" HorizontalAlignment="Center" FontFamily="{StaticResource FontIrish}">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="5"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock x:Name="FinalScoreText" Margin="0,0,0,40" Text="00 - 00" Foreground="{StaticResource ColorTextYellow}" FontSize="50" HorizontalAlignment="Center"/>

                <Button x:Name="buttonRejouerGameOverScreen" Content="REJOUER" Click="buttonRejouer_Click" Style="{StaticResource StyleBtnMenu}" Background="{StaticResource ColorAlert}"/>
                <Button x:Name="buttonMenuGameOverScreen" Content="MENU" Click="buttonQuitter_Click" Style="{StaticResource StyleBtnMenu}" Background="{StaticResource ColorAlert}"/>
            </StackPanel>
        </Grid>

        <Grid x:Name="canvasMenuPause" Visibility="Collapsed" Background="{StaticResource ColorOverlay}">
            <Grid Width="726" Height="492" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Menu Pause" Margin="189,10,0,0" FontFamily="{StaticResource FontIrish}" FontSize="64" Foreground="{StaticResource ColorTextYellow}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                <TextBlock x:Name="textScore" Text="0 : 0" Margin="0,118,0,0" FontFamily="{StaticResource FontIrish}" FontSize="32" Foreground="{StaticResource ColorAlert}" HorizontalAlignment="Center" VerticalAlignment="Top"/>

                <Image Source="/images/logo_chronodunk.png" Width="319" Height="174" Margin="475,202,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <RotateTransform Angle="15.6"/>
                    </Image.RenderTransform>
                </Image>
                <Image Source="/images/personnage_chronodunk.png" Width="366" Height="396" Margin="-76,96,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" RenderTransformOrigin="0.5,0.5">
                    <Image.RenderTransform>
                        <RotateTransform Angle="7.8"/>
                    </Image.RenderTransform>
                </Image>

                <StackPanel Margin="244,191,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Button x:Name="buttonReprendre" Content="REPRENDRE" Click="buttonReprendre_Click" Style="{StaticResource StyleBtnMenu}"/>
                    <Button x:Name="buttonOptions" Content="OPTIONS" Click="ButtonOptions_Click" Style="{StaticResource StyleBtnMenu}"/>
                    <Button x:Name="buttonQuitter" Content="QUITTER" Click="buttonQuitter_Click" Style="{StaticResource StyleBtnMenu}"/>
                </StackPanel>
            </Grid>
        </Grid>

        <Grid x:Name="MenuOptions" Visibility="Collapsed" Background="#E6000000">
            <Border Width="600" Height="480" CornerRadius="20" BorderBrush="{StaticResource ColorPrimary}" BorderThickness="3" Background="Black" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Margin="30">
                    <TextBlock Text="OPTIONS" FontFamily="{StaticResource FontIrish}" FontSize="50" Foreground="{StaticResource ColorPrimary}" HorizontalAlignment="Center" Margin="0,0,0,40"/>

                    <Grid Margin="0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Difficulté :" Foreground="White" FontSize="22" VerticalAlignment="Center"/>
                        <ComboBox x:Name="DifficulteIA" SelectionChanged="ComboDifficulty_SelectionChanged" Grid.Column="1" FontFamily="{StaticResource FontIrish}" FontSize="18" Height="40" VerticalContentAlignment="Center">
                            <ComboBoxItem Content="Facile"/>
                            <ComboBoxItem Content="Normal"/>
                            <ComboBoxItem Content="NBA Star"/>
                        </ComboBox>
                    </Grid>

                    <Grid Margin="0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Contrôles :" Foreground="White" FontSize="22" VerticalAlignment="Center"/>
                        <ComboBox x:Name="ChoixControles" Grid.Column="1" FontFamily="{StaticResource FontIrish}" FontSize="18" Height="40" VerticalContentAlignment="Center" SelectedIndex="0">
                            <ComboBoxItem Content="Flèches"/>
                            <ComboBoxItem Content="ZQSD"/>
                        </ComboBox>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,50,0,0">
                        <Button Content="VALIDER" Click="ButtonValider_Click" Style="{StaticResource StyleBtnMenu}" Width="180" Background="#00AA00" Margin="10,0"/>
                        <Button Content="RETOUR" Click="ButtonRetourOptions_Click" Style="{StaticResource StyleBtnMenu}" Width="180" Margin="10,0"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>